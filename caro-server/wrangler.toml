name = "caro-server"
main = "src/index.js"
compatibility_date = "2024-05-12"

[vars]
# Restrict to your frontend origin in production, e.g. "https://<user>.github.io".
# Leave as "*" only for local dev.
ALLOWED_ORIGIN = "*"

[[durable_objects.bindings]]
name = "ROOMS"
class_name = "RoomDurableObject"

[[migrations]]
tag = "v1"
new_classes = ["RoomDurableObject"]
