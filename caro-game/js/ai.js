// ai.js — heuristic AI move selection (implemented in Phase 4)

import { BOARD_SIZE } from './game.js';

export function pickMove(_game) {
  // Stub: returns null until Phase 4 implements pattern scoring.
  return null;
}

export function _internals() {
  return { BOARD_SIZE };
}
